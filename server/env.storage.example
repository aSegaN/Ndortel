# ============================================
# FICHIER: .env.storage.example
# CHEMIN COMPLET: server/.env.storage.example
# DESCRIPTION: Variables d'environnement pour SEC-004
# ============================================

# ============================================
# MINIO / S3 CONFIGURATION
# ============================================

# Endpoint MinIO (sans http://)
MINIO_ENDPOINT=localhost

# Port API S3
MINIO_PORT=9000

# Utiliser SSL (true en production)
MINIO_USE_SSL=false

# Credentials MinIO
# ⚠️ CHANGER EN PRODUCTION!
MINIO_ACCESS_KEY=ndortel-admin
MINIO_SECRET_KEY=SecureMinioPass2025!

# Région
MINIO_REGION=sn-dakar-1

# Buckets
MINIO_BUCKET_BIOMETRIC=biometric-documents
MINIO_BUCKET_HOSPITAL=hospital-certificates
MINIO_BUCKET_AUDIT=audit-logs

# ============================================
# CHIFFREMENT AES-256
# ============================================

# Clé maître de chiffrement (32 caractères minimum)
# ⚠️ CRITIQUE: Générer une clé unique en production!
# Utiliser: openssl rand -base64 32
STORAGE_ENCRYPTION_KEY=your-32-character-encryption-key!

# ============================================
# PRODUCTION CHECKLIST
# ============================================
# 
# 1. Générer une vraie clé de chiffrement:
#    openssl rand -base64 32
#
# 2. Configurer MinIO avec SSL:
#    MINIO_USE_SSL=true
#
# 3. Utiliser des credentials sécurisés:
#    MINIO_ACCESS_KEY=<généré>
#    MINIO_SECRET_KEY=<généré avec openssl rand -base64 32>
#
# 4. Activer le chiffrement KMS MinIO en production
#
# 5. Configurer les sauvegardes MinIO
